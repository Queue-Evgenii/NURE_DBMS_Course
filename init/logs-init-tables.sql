CREATE TABLE grants_log (
  log_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  action VARCHAR2(10) CHECK (action IN ('INSERT', 'UPDATE', 'DELETE')),
  grant_id VARCHAR2(20),
  change_date TIMESTAMP DEFAULT SYSDATE
);